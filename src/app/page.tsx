/*
 * Homepage placeholder — Step MS.2 establishes the base layout (Nav +
 * Footer) only. The Hero and feature sections, with their anchor
 * targets (#how-it-works, #features, #faq, #pricing), are built in
 * Steps MS.3–MS.7.
 */

export default function Home() {
  return (
    <div className="mx-auto flex max-w-6xl flex-col items-center gap-4 px-6 py-24 text-center">
      <p className="text-lg font-medium text-muted">
        [Placeholder — homepage sections are built in Steps MS.3–MS.7.]
      </p>
    </div>
  );
}
